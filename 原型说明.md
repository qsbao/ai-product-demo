# 原型说明：时刻设计的可交互验证

> 配套文件 [`原型.html`](原型.html)。单文件、零依赖、双击即在 Chrome/Edge 打开（刷新即重置数据）。
> 它把 [时刻设计.md](时刻设计.md) 的「角色 × 时刻」网格做成可点的界面，验证三件事：
> 时刻链点得通、主动作一键可达、通知/权限作为副产物自动浮现。
> 概念基础见 [领域模型.md](领域模型.md)，方法基础见 [产品设计方法.md](产品设计方法.md)。

---

## 一、它是什么 / 不是什么

- **是**：8 个 Tier-2「时刻视图」+ 1 张代表性 Tier-1 实体表（Issue 全表）+ 5 个角色切换的高保真交互原型；操作**真改内存状态并跨视图联动**（升级告警→工作台多一条 Issue→提交审批→审批队列变化 + PI 评论队列同时机变化→打回→工作台置顶……整条链点得通）。
- **不是**：不是真系统——无后端、无持久化、无登录（角色用左上下拉模拟），刷新即回到初始 mock。
- **只放一张 Tier-1 表**：Issue 全表用来演示 Tier-1↔Tier-2 的接缝（见第五节）；其余实体表（Alarm/议题/Meeting/Event/报告/参考数据）仍属长尾，落地时由通用生成器产出，原型不铺开。

打开后默认进入「工程师 · 分诊收件箱」。顶栏有通知铃（未读数）和「重置数据」。

---

## 二、5 个角色（左上角切换）

| 角色 | 性质 | 时刻视图 | 一句话 |
|------|------|---------|--------|
| **工程师** | 高频·主角色 | 分诊收件箱、Issue 工作台 | 驱动 Issue 的人——系统的重力中心 |
| **审批经理** | 事件驱动·低频 | 待我审批 | 被"提交审批"卷入，轮到我才来 |
| **PI（制程整合工程师）** | 事件驱动·低频·**与审批并行** | 待我评论 | 与「提交审批」同时机卷入，写**参考意见，不阻塞经理**（决策 #12） |
| **会议主持人** | 周期驱动 | 待汇报清单、会议议程 | 绕着 Meeting/议题 转，会前+会中（议题=原 Report 改名） |
| **管理员** | 第二用户类型 | 接入健康（+参考数据） | 维护"系统能不能跑"，不碰业务 Issue |

「通知中心」是跨角色的，每个角色都能进，只显示"冲着当前角色来的事"。

---

## 三、8 个 Tier-2 视图

| # | 视图 | 角色·时刻 | 一键主动作 | 触发的跨视图联动 |
|---|------|----------|-----------|-----------------|
| 1 | **分诊收件箱** | 工程师·入口 | 升级为 Issue / Ack / 忽略 | 升级→告警出列 + 工作台新增 Issue（owner 按注册表解析、审批链 + PI 快照、落创建事件） |
| 2 | **Issue 工作台** | 工程师·推进 | 提交审批（随状态变化为单个按钮，已解决为"等上会"相位无主动作） | 审批通过自动建议题+自动排期；按"需我动作+超时"自动置顶；含 全部/必须上会/Kanban/超时/已升级 过滤页签；行内 📄 报告角标（决策 #11） |
| 3 | **事件时间线** | 工程师·卡点 | （只读追溯，嵌在 Issue 详情抽屉「事件」页签） | 展示状态/负责人/来源 + 审批链快照 + PI 评论事件 + 议题历史 + 报告生成事件 + append-only 事件流 |
| 4 | **待我审批** | 审批经理 | 批准 / 打回（带意见） | 批准→游标 +1 或到顶则自动推进（✓ 路径已解决+自动建议题；✗ 路径直关）；打回→Issue 回「处理中」+ 工作台置顶 + 通知工程师。行内带 **PI 已评 N/M 辅助列**（决策 #12，仅辅助不阻塞） |
| 5 | **待我评论** | PI | **写技术意见（必填正文）** | 落 `pi-comment` 事件进时间线 + 待我审批面板"PI 已评"+1；**不影响经理审批出口**（不阻塞）。可在审批通过后补评。 |
| 6 | **待汇报清单** | 会议主持人·会前 | 重排到别会 | 每条议题旁带 📄 报告 ready/未 ready 角标，可催 owner |
| 7 | **会议议程** | 会议主持人·会中 | 记结论：成功 / 需重排 / 驳回 | 成功→Issue 关闭；需重排→**新建**议题（旧标"需重排"，历史不可变）；驳回→Issue 退回处理中。报告文档不动（决策 #11） |
| 8 | **通知中心** | 跨角色·早间 | 点任一条 → 直达对应时刻视图并高亮 | 聚合当前角色的所有"需我动作"，已读计数同步顶栏铃 |

> 对照 [时刻设计.md](时刻设计.md) 的收口：#3 事件时间线、以及 #6/接入健康同属"派生看板族"，都按设计内嵌在所属视图里，不另立导航——所以导航上看到的是 8 个聚焦入口，背后是网格里全部时刻。

设计纪律在界面上的体现：**每行只暴露当前状态下的那一个主动作**，其余信息折叠进详情抽屉；落地页不堆 widget。

### 详情：抽屉（分页签）+ 整页详情页

Issue 详情内容很多（描述/根因、受影响批次、关键参数量测、关联告警、附件、讨论、审批链、事件时间线、议题历史、报告文档外链）。原型用**两种载体**承载，由任务而非内容量决定：

- **抽屉（推进用，默认）**：内容按 **概览 / 事件 / 关联 / 讨论** 4 个分页签裁剪折叠，主动作常驻底部；点开不离开当前列表，ESC 即回队列接着下一条。这是工程师 99% 日常的场景。
- **整页详情页（研究/分享/打印用）**：抽屉底部「⤢ 展开为整页」进入 `Issue 详情页`，所有区块全量展开 + 模拟深链地址（可收藏/分享/打印）。它和抽屉**共用同一组区块渲染器**，只是布局壳不同——一份内容、两处不漂移。

> 判据见 [产品设计方法.md](产品设计方法.md) 的「抽屉 vs 详情页——由任务决定，不由内容量决定」：内容多先用渐进披露（分页签）解决，只有任务变成"深链/打印/并排/超重内容"才升级整页。

---

## 四、推荐演示动线（约 1 分钟，点出整条时刻链）

1. **工程师 / 分诊收件箱**：对带「可聚合 ×2」的告警点「升级为 Issue」→ 告警出列；切到「Issue 工作台」见新 Issue（负责人 李工 已按 site→owner 注册表解析；同时 PI 已按 site→PI 注册表 snapshot）。
2. **工作台**：对该 Issue 点「提交审批」→ 状态变「审批中」，顶栏铃为审批经理 +1，**PI 队列同时机 +1**（并行）。
3. **切到 PI（默认 林工）/ 待我评论**：对刚提交的 Issue 写一条「技术意见」→ 落 `pi-comment` 事件、待我评论队列出列；**不影响经理审批出口**——经理仍可在 PI 未评时就批。
4. **切到审批经理 / 待我审批**：行内可见「PI 已评 N/M + 摘要」辅助列（决策 #12，仅辅助不阻塞）。对该 Issue 点「打回」填意见 → 切回工程师工作台，该 Issue 📌 置顶、时间线多一条打回事件、工程师收到通知。
5. 重新提交 → 审批经理逐级「批准」到链顶 → **自动推进**：必须上会=✓ 进「已解决」+ 自动建议题 + 自动排期；必须上会=✗ 直接「已关闭」。
6. **切到会议主持人 / 待汇报清单**：看到该 Issue 的议题已自动挂到目标早会、旁带 📄 报告角标 → 点「重排」挪到别会 → 出现**新的**议题，旧记录仍在（重排留痕；报告文档不动）。「会议议程」逐条记结论：成功→Issue 自动关闭；驳回→Issue 退回处理中。
7. **切到管理员 / 接入健康**：把 FDC 数据源「重试同步」由失败变正常；在参考数据面板改某站点负责人，再回分诊升级新告警验证解析变化（PI 注册表同理）。
8. **通知中心**：点任一通知 → 跳到对应视图并高亮目标项，已读计数清零。

---

## 五、关于实体表（Tier 1）：原型为什么没有，何时要补

**视图（Tier 2 时刻视图）和实体表（Tier 1 通用 CRUD）是两类东西，并存、各管一摊。**

| | Issue 工作台（Tier 2） | Issue 全表（Tier 1，已加入原型） |
|---|---|---|
| 回答 | "轮到我的、该我动的，下一步点哪" | "所有 Issue，按任意条件查/排序/导出/补录" |
| 范围 | 过滤后（我名下、未关闭、按需动作排序） | 全量、全字段（**含已关闭**） |
| 主语 | 推进（一键主动作） | 浏览 / 检索 / 管理 |
| 用户 | 工程师日常 | 经理看全局、管理员补数、出报表、找不在我流程里的那条 |
| 导航位置 | 工程师 · 时刻 组 | 左下「TIER-1 实体表」组（跨角色，任何角色都能进） |

**原型里的 Issue 全表**（左下导航）正是这条接缝的演示：表格形态明显区别于卡片式时刻视图；带搜索 + 状态过滤 + 导出按钮；行**只读不放操作**，点任一行进入和「Issue 工作台」**共用的同一个详情抽屉**——驱动业务的动作只在抽屉里完成；抽屉里再点「⤢ 展开为整页」可进入全量 `Issue 详情页`（适合从全表做深度查阅/分享/打印）。它含已关闭 Issue（工作台会隐藏），口径与工作台不同。

结论：

- **Issue 确实需要一张实体表**，但它不是第二个工作台——**只读为主、不在表里驱动业务**；点行 → 同一个详情抽屉。原型已落这一张作样板。
- 其余需要表的实体（都属 Tier 1，原型未铺开）：**Alarm**（只读事实归档，补分诊收件箱看不到的历史/全量）、**议题/Meeting/Event**（审计、排期 CRUD、全局审计日志）、**报告**（外链薄壳，1:1 Issue，由外部报告服务建/管，本系统持 url+状态——决策 #11）、**参考数据**（site→owner、审批阶梯，纯 CRUD）。
- **其余表为何不在原型里**：Tier 1 是"概念→表→详情"生成器层，设计风险低、价值密度低（≈10%），是方法里确定能做对的部分；落地时由通用生成器一次性产出，管理员是主要消费者。原型只留 Issue 全表一张作为接缝样板。

---

## 六、实现备忘（给开发团队）

- 单文件结构：`<style>` 设计系统 / `<body>` 三栏骨架（侧栏角色+导航、顶栏、主区） / `<script>` = `store`(单一可变状态) + `derive`(派生函数 f(实体+规则+事件)) + `RULES`(硬编码 SLA/审批阶梯/注册表) + `VIEWS[]`(每视图一个渲染函数) + 路由。
- 与领域模型一致的关键实现：多级审批 = 单一「审批中」状态 + `currentLevel` 游标（非多状态）；重排 = 新建议题不改旧的；事件 append-only；事实字段只读、人只写运营字段；报告 = 1:1 Issue 的外部权威源薄壳（决策 #11）；**PI 会评 = `piReviewers` 创建时 snapshot + `pi-comment` append-only 事件，不进状态机出口**（决策 #12）。
- 已通过确定性测试（升级/审批/打回/自动推进/议题排期/重排/驳回/同步全链路 + 7 视图渲染断言全过）。
