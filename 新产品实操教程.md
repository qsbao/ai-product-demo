# 新产品实操教程：把「概念识别 + 时刻设计」套到下一个产品

> 这是一份**照着做**的 playbook，不是理论复述。目标：拿到任意一个新内部系统（工单 / 审批 / 运维 / ATS / 报销…），按固定流水线走到一份可交互原型，每一步都有**模板可填**、有**判据可验**、有**反模式可避**。
> 理论总纲见 [产品设计方法.md](产品设计方法.md)；本仓库的半导体范例见 [领域模型.md](领域模型.md) / [时刻设计.md](时刻设计.md) / [原型说明.md](原型说明.md)。
> 本教程把那套范例**抽象成可迁移的步骤**，并在 [附录 B](#附录-b换一个域走一遍ats证明方法不依赖半导体) 用一个全新域（招聘 ATS）从头走一遍，证明方法不绑定半导体。

---

## 0. 心法（先背下这三句）

> **① 产品设计 = 概念识别（名词）＋ 时刻设计（动词）。**
> **② 两层 UI：Tier-1 通用 CRUD（60% 屏 / 10% 价值）＋ Tier-2 时刻视图（40% 屏 / 90% 价值）。胜负全在那 40%。**
> **③ 形态即契约：卡片 = 动手，表格 = 查数。用户没读字，就该从样子知道"我在哪层、此刻该干嘛"。**

| | 概念识别 | 时刻设计 |
|---|---|---|
| 回答 | 系统里**有什么** | 用户此刻**要做什么** |
| 产出 | 领域模型：实体 / 关系 / 生命周期 / 规则 | 角色 × 时刻 → 视图清单 |
| 决定 | **对错**（装不装得下业务） | **生死**（打不打得过 Excel / Jira） |
| 你的状态 | 多数架构师已会 | **这才是要刻意补的独立工序** |

---

## 适用判定（先确认你该不该用这套方法）

| 该用 | 别用（用别的方法） |
|---|---|
| 内部工具、多角色协作 | 面向消费者 / 增长型产品 |
| 有工作流 / 审批 / 周期性例程 | 纯展示、纯字典、纯报表 |
| 目标是"战胜 Excel / Jira / 邮件" | 单角色、单动作、无状态推进 |
| 数据一部分来自上游事实、一部分人工运营 | 数据全人工录入，无上下游 |

> 判定通过 → 往下走。判定不通过而硬套，会做出一堆"没人来看"的角色 dashboard。

---

## 流水线总览（六阶段，有门禁，不可跳）

| 阶段 | 你在做什么 | 产出 | 门禁（过不了别进下一阶段） |
|---|---|---|---|
| **1 概念识别** | 建领域模型 | 实体链 + 字段二分 + 状态流转表 | 找到唯一"引力中心"；事实/运营字段分清 |
| **2 时刻设计** | 角色 × 时刻推导 | 时刻网格 → ~N 个 Tier-2 视图 | 每个高频时刻 ≤1 视图、主动作一键直达 |
| **3 分层与形态** | 给每个视图定层级 + 形态 | 卡片/表格归类表 + 视线检查 | 每个新屏的形态由"它属哪层"决定，非拍脑袋 |
| **4 详情载体** | 抽屉 vs 详情页 | 区块渲染器清单 | 由"用户此刻在干嘛"决定，非字段数 |
| **5 副产物** | 通知 + 权限 | 通知通道表 + 权限矩阵 | 两者从网格列**读出**，未单独立项 |
| **6 原型验证** | 单文件可点原型 | `原型.html` + 演示动线 + 测试 | 整条时刻链点得通、跨视图联动 |

**核心顺序铁律**：① 必须先有引力中心才能选主角色；② 必须先打穿主角色时刻链，低频角色才能"派生"；③ 通知和权限是副产物，永远最后从网格里读，不准开头就设计 RBAC。

---

## 阶段 1 · 概念识别（建领域模型）

> 你多半已会，这里只给**新产品最易翻车的 6 条纪律**和填空模板。范例见 [领域模型.md](领域模型.md)。

### 1.1 六条必守纪律

| # | 纪律 | 一句话 | 翻车长什么样 |
|---|---|---|---|
| 1 | **找唯一引力中心** | "人每天驱动的那个实体"，不是数据量最大的那个 | 把只读证据（告警/简历）当中心，给它配富工作台 |
| 2 | **事实字段 vs 运营字段** | 上游来的只读（同步会覆盖）；人只写运营层 | 让人去编辑会被下次同步冲掉的字段 |
| 3 | **规则一律硬编码** | SLA / 超时 / 升级 / 节奏，集中显式、改代码发版 | 过早做规则引擎 + 配置 UI（成本无人偿付） |
| 4 | **状态是枚举，不是引擎** | 少量状态 + 一张合法流转表 | 上工作流引擎、把流程拓扑暴露给用户 |
| 5 | **多级 = 游标，不是多状态** | 同一个"审批中" + `current_level` 游标 | 把四级/五级审批做成 5 个状态（状态爆炸） |
| 6 | **事件做 A 型审计** | 状态照存，**额外**追加不可变事件流喂时间线 | 上 Event Sourcing（运营系统灾难性过度设计） |

> 心法：**详情页该多能动手，取决于"这概念上发生多少人的操作"，而非"它有多少属性"。**
> 心法：**阶段（phase）才配当状态，迭代（iteration）只配当数据。别把数据建成拓扑。**
> 心法：凡 `f(实体 + 规则 + 事件)` 算出、**无独立生命周期**的（超时清单 / 待办 / 升级清单）= **派生视图，不是概念**。别硬做成表。

### 1.2 模板（复制填空）

**实体链**（画出"人沿哪条链推进"）：
```
__上游只读事实__ ──(聚合/转化)──> __引力中心(工作流)__ ──(需周期处理)──> __连接实体__ ──(排期于)──> __周期容器__
                                          │
                                   事件 Event（旁挂 append-only 时间线/审计）
```

**字段二分表**（每个实体都过一遍）：

| 实体 | 事实字段（只读·有来源） | 运营字段（人可写·本系统拥有） |
|---|---|---|
| | | |

**状态流转表**（只给引力中心做）：

| 当前状态 | 允许的下一状态 | 触发动作 | 谁能触发 |
|---|---|---|---|
| | | | |

### 1.3 判据 ✅

- [ ] 能一句话说出引力中心是谁，且它是"人每天动手最多"的那个。
- [ ] 每个实体的字段都被分进"事实"或"运营"，没有第三类。
- [ ] 多级审批写成了"1 状态 + 1 游标"，状态总数 ≤ 6。
- [ ] 所有"清单/看板"先被质疑过："你是概念还是 `f(实体+规则+事件)`？"

---

## 阶段 2 · 时刻设计（六步纪律）

> 这是**动词那一半**，决定生死。范例推演见 [时刻设计.md](时刻设计.md)。
> 核心：时刻设计不是"给每个角色画几个页面"，而是一条**有严格依赖顺序**的推导，每步都砍掉一堆"看似合理实则有害"的设计。

### 步骤 1 — 选主角色（对齐引力中心）

不要 5 个角色平行铺开（对称陷阱）。选**和引力中心对齐的那个高频角色**，先把它的时刻链打穿，其余角色**派生**。

- 判据：主角色 = 驱动引力中心的人 + 高频（每天反复进出）。
- 范例：引力中心是 Issue → 主角色是**工程师**（驱动 Issue），不是告警值班、不是审批经理。

### 步骤 2 — 走主角色的「入口 → 推进 → 卡点」

贴着概念链走叙事："早上来先干什么（入口）→ 然后干什么（推进）→ 卡在哪（卡点）"。

| 阶段 | 问的那一句 | 配什么 |
|---|---|---|
| **入口** | "冲我来了什么？哪些要处理、哪些可忽略？" | 半自动分诊：机器能定的自动做，**拿不准的才进池** |
| **推进** | "我名下哪个该我现在动手？推到下一状态。" | 富工作台：按"需我动作 + 快超时"置顶；主动作随状态变成**单个**按钮 |
| **卡点** | "这条怎么走到今天的？为什么被打回？" | 只读事件时间线（嵌详情）——卡点解法是"看清"，动作回工作台 |

> 纪律：**每个时刻只问一句**——他此刻要做的那**一个**决定是什么？配"主动作一键可达 + 最小必要信息 + 其余全隐藏"。

### 步骤 3 — 低频角色按"如何被卷入"派生（不给它造早间入口）

审批经理 / 会议主持人不是每天主动来的，他们被主角色的**状态变更卷入**。最佳形态是 **一条通知 + 一个 whose-turn（轮到我）视图**，不是独立 dashboard。

- 多级审批**不产生多视图**：四级/五级看到的是同一个"待我审批"，区别只是 `current_level` 游标走到了谁。
- 卡点（信息不足）→ 打回 = 生成一个 Event + 通知回主角色（落到他工作台"需我动作"置顶）。

### 步骤 4 — 每个时刻 → 一个聚焦视图

主动作一键完成，其余全部隐藏/折叠。**落地页只能有一个赢家，不许塞 widget。**

### 步骤 5 — 收口去重 → ~N 个 Tier-2 视图

把所有时刻视图去重，多个角色常共享同一视图，自然收敛（范例收敛到 7 个）。剩下长尾掉进 Tier-1 CRUD。

### 步骤 6 — 让副产物浮现（先占位，阶段 5 再展开）

- **通知通道** = 步骤 3 里所有"把人卷回来"的触发点的集合。
- **权限** = 每个视图只暴露该时刻需要的那一个写动作。
- 两者都**不单独立项**。

### 模板 — 角色 × 时刻网格（核心交付物，复制填空）

| 角色 | 性质 | 时刻（入口/推进/卡点 或 唯一卷入点） | 主动作（一键） | 通知触发 | 隐含写权限 |
|---|---|---|---|---|---|
| **（主角色）** | 高频 | 入口· | | | |
| **（主角色）** | 高频 | 推进· | | | |
| **（主角色）** | 高频 | 卡点· | （只读追溯） | — | 只读 |
| （低频角色） | 事件驱动 | 轮到我· | | | |
| （周期角色） | 周期驱动 | 会前· | | | |
| （周期角色） | 周期驱动 | 会中· | | | |
| （管理员） | 第二用户类型 | 看健康 / 维护参考数据 | | | |
| **跨角色** | 早间粘合 | 看冲我来的事 | 点击直达对应视图 | digest + push | 只看自己被卷入的 |

### 本阶段主动规避的反模式

| 反模式 | 表现 | 为什么有害 |
|---|---|---|
| **对称陷阱** | 给每个角色发一个 dashboard | 低频角色根本不来看，价值为零，还稀释主角色设计 |
| **widget 堆砌** | 落地页塞满"我的 X / 我的 Y"卡片 | 退化成又一个 Excel/Jira，没有"一键主动作" |
| **视图升表** | 把"超时清单/待汇报"做成实体表 | 它是派生视图，不是概念 |
| **状态爆炸** | 把多级审批做成多个状态 | 应是 1 状态 + level 游标 |
| **低频当高频** | 给审批经理设计"早间入口" | 他没有早间例程，他是被通知拉进来的 |

### 判据 ✅

- [ ] 主角色对齐引力中心，且时刻链（入口→推进→卡点）能讲成一段叙事。
- [ ] 每个角色 2–3 个高频时刻，**没有**对称式 dashboard。
- [ ] 每个时刻 1 个视图，主动作一键，其余隐藏。
- [ ] 低频角色是"通知 + whose-turn"派生出来的，没有独立早间入口。
- [ ] 收口到 ~N 个 Tier-2 视图，长尾交给 Tier-1。

---

## 阶段 3 · 分层与形态（把视图落成像素）

> **形态本身是契约**：两层不只"做什么"不同，**长什么样**也固定成两套视觉语言。

### 3.1 两套形态

| | Tier-2 时刻视图 | Tier-1 实体表 |
|---|---|---|
| 形态 | 卡片 / 行列表，每行**一个主按钮**，详情折叠 | 密集表格，多列，可排序/筛选/导出 |
| 优化的认知任务 | **决策 + 动作**（视线落按钮） | **定位 + 比较**（视线扫列） |
| 可写性 | 在此动手 | 行只读，点行进详情/工作台才动手 |

为什么值得当纪律：**省重学**（全 app 只学一次"卡片=动手、表格=查数"）；**任务不可混用**（表格做时刻视图→主动作淹没在列里；卡片堆千条→没法扫/比/筛）；**防滑回 Excel**（让时刻视图刻意不像表格）。

### 3.2 形态由"哪层"决定 —— 决策检查

**新屏判层** → **判层定形态**：

```
这个屏服务"用户此刻推进一个决定/动作"吗？
  ├─ 是 → Tier-2 → 卡片/行列表 + 单主按钮 + 详情折叠
  └─ 否（是浏览/检索/补录/管理全量）→ Tier-1 → 密集表格 + 排序筛选导出 + 行只读
```

### 3.3 视线杠杆（形态为什么起作用：它在控制视线路径）

| 杠杆 | 时刻视图（要"动手"） | 实体表（要"找/比"） |
|---|---|---|
| 颜色饱和 | 只给主动作上饱和色 → 当"视线磁铁" | 几乎不用，保持平静好扫 |
| 位置 | 该动手的放左上、动作放右端（行的终点） | 表头在顶当标签锚 |
| 对齐 | 标题左对齐 → 稳定左边缘下扫 | 数字右对齐 → 对位比大小 |
| 排序/筛选 | **最强**：把答案排到眼睛落点，免扫 | 列排序/搜索做定点定位 |
| 密度 | 低密度，每条给一次注视 | 高密度，进入扫描模式 |

> **关键反推**：实体表行内**故意不放饱和色动作按钮**——否则前注意的"跳"触发几十次，把本该平静的比较界面搅烂。所以行只读、动作延后到抽屉。
> 一句话：**形态决定视线路径，视线路径决定任务效率。** 选卡片还是表格，本质是在选"优化哪种眼动"。

### 判据 ✅

- [ ] 每个新屏先判层、再定形态，没有"内容多就上表格"式拍脑袋。
- [ ] Tier-2 视图里只有主动作是饱和色；Tier-1 表里行内无磁铁色按钮。
- [ ] "该我动的"用**排序**置顶（排序替用户把扫描做完了——优化天花板）。

---

## 阶段 4 · 详情载体（抽屉 vs 详情页：由任务决定，不由内容量决定）

> 最常见的实操疑问：实体字段特别多，抽屉装得下吗？要不要整页详情页？
> **易犯的错是用"内容量"做判据**。正确判据仍是**用户此刻在干嘛**。

| | 抽屉（Tier-2 时刻配件） | 详情页（Tier-1 规范记录） |
|---|---|---|
| 服务的时刻 | **推进**——在我清单里顺手处理这一条 | **研究/追溯/分享/打印**——停下吃透或对外引用 |
| 核心价值 | **不离开上下文**：ESC 即回队列，接着下一条 | **独立地址**：可收藏/分享/打印/并排比对 |
| 内容策略 | 按此刻裁剪 + **分页签**，低频字段折叠 | 全字段全展开，不裁剪 |
| 入口 | 从工作台/收件箱/通知点开 | 从全表、深链、或抽屉里"展开整页" |

要点：

- **内容多，先用渐进披露（分页签 + 段内分区）解决，而不是换载体**。抽屉纵向空间很大，几十个字段也扛得住。
- **真正逼出整页的不是字段数，是任务变了**：要深链/分享、打印归档、并排比对、超重内容（大量附件/关联）。
- **一份内容，两种载体，别写两套**：用同一组**区块渲染器**，抽屉调它（裁剪+分页签）、详情页也调它（全量展开），区别只在布局壳。否则字段一改两处漂移。
- **形态依旧在说话**：浮层 = 在流程里顺手动手，整屏 = 停下研究一条记录。

### 判据 ✅

- [ ] 默认抽屉 + 分页签；只有当任务变成"深链/打印/并排/超重"才升整页。
- [ ] 抽屉和详情页共用同一组区块渲染器，没有第二套字段实现。

---

## 阶段 5 · 副产物（通知 + 权限，从网格列读出）

> 不单独设计。它们就是阶段 2 网格里两列的直接读出。

### 5.1 通知通道 = 网格"通知触发"列的集合

| 触发事件 | 通知谁 | 落到哪个时刻视图 | 通道 |
|---|---|---|---|
| | | | |

> 原则：**高频角色用站内 + 早间 digest 即可；低频/卷入式角色（审批/汇报/值守）必须外部 push**，否则他们不会回来。

### 5.2 权限 = 视图可见性 + 视图内动作写权限（不需独立 RBAC）

- **事实字段全局只读**（上游来，同步可能覆盖）；人只写运营字段。
- 每个时刻视图**只暴露该时刻需要的那一个写动作**——天然最小权限。
- 角色边界 = "能看到哪些时刻视图 + 能按哪个动作键"。

| 角色 | 能进的时刻视图 | 能按的写动作 |
|---|---|---|
| | | |

### 判据 ✅

- [ ] 通知表每一行都能在网格里找到对应的"卷回来"触发点。
- [ ] 没有写一行独立 RBAC；权限完全是"视图 × 动作"的副产物。

---

## 阶段 6 · 原型验证（一个文件，可点）

> 目标不是真系统，是**可交互验证三件事**：时刻链点得通、主动作一键可达、通知/权限作为副产物自动浮现。范例见 [原型说明.md](原型说明.md) + `原型.html`。

### 6.1 单文件骨架

```
原型.html（零依赖，双击即开，刷新即重置）
├─ <style>  设计系统：两套形态的视觉语言（卡片 vs 表格）
├─ <body>   三栏骨架：侧栏(角色切换+导航) / 顶栏(通知铃+重置) / 主区
└─ <script>
     store    单一可变状态（内存 mock）
     derive   派生函数 f(实体+规则+事件)  ← 超时/升级/待办都在这算，不入库
     RULES    硬编码 SLA / 审批阶梯 / 参考数据注册表
     VIEWS[]  每个 Tier-2 视图一个渲染函数 + 一张代表性 Tier-1 表
     router   角色 × 视图路由
```

### 6.2 落地必须与领域模型一致的关键点

- 多级审批 = 单一「审批中」状态 + `currentLevel` 游标（**非**多状态）。
- 重排/重试 = **新建**记录不改旧的（历史不可变留痕）。
- 事件 append-only；事实字段只读、人只写运营字段。

### 6.3 验收：整条链点得通（演示动线模板）

> 串成一条 ~1 分钟动线，证明跨视图联动：
> 入口处理一条 → 引力中心实体 +1 → 提交审批 → 审批队列变化 → 打回 → 工作台置顶 + 通知 → 重新提交逐级批准 → 推进到周期处理 → 重排生成新记录旧的仍在 → 管理员改参考数据后回入口验证解析变化 → 通知中心点任一条直达高亮。

### 判据 ✅

- [ ] 一个操作能跨多个视图联动（不是各视图各演各的）。
- [ ] 派生清单全部 `derive()` 算出，没有把派生视图写成存储表。
- [ ] 通过确定性测试（全链路状态机 + 各视图渲染断言）。

---

## 全程反模式速查表（贴墙用）

| 阶段 | 反模式 | 正解 |
|---|---|---|
| 概念 | 把只读证据当引力中心 | 中心 = 人每天驱动的实体 |
| 概念 | 多级审批做成多状态 | 1 状态 + level 游标 |
| 概念 | 过早做规则引擎/配置 UI | 规则硬编码，等"高频变更×必须自助"铁证 |
| 概念 | 上 Event Sourcing | A 型审计：存状态 + 旁挂事件流 |
| 时刻 | 每个角色一个 dashboard | 选主角色打穿，低频角色派生 |
| 时刻 | 落地页堆 widget | 一页一个赢家，主动作一键 |
| 时刻 | 派生清单升成实体表 | 它是 `f(实体+规则+事件)` |
| 时刻 | 给低频角色造早间入口 | 通知 + whose-turn |
| 形态 | "内容多就上表格" | 先判层再定形态 |
| 形态 | 实体表行内放磁铁色按钮 | 行只读，动作延后到抽屉 |
| 详情 | 字段多就上整页 | 先渐进披露；任务变了才升整页 |
| 详情 | 抽屉和详情页写两套 | 共用区块渲染器 |
| 副产物 | 开头就设计 RBAC | 权限从"视图×动作"读出 |

---

## 完工自检清单（全流程）

- [ ] 引力中心唯一、且是高频驱动实体。
- [ ] 字段二分（事实/运营）干净，状态 ≤6 且多级用游标。
- [ ] 主角色对齐引力中心，时刻链是一段叙事而非页面清单。
- [ ] 每个时刻一视图、主动作一键、其余隐藏；无对称 dashboard、无 widget 堆砌。
- [ ] 收口到 ~N 个 Tier-2 视图，长尾交 Tier-1。
- [ ] 每个新屏先判层再定形态；Tier-2 只有主动作上饱和色。
- [ ] 详情默认抽屉+分页签，整页只为"深链/打印/并排/超重"；共用区块渲染器。
- [ ] 通知/权限从网格列读出，未单独立项。
- [ ] 单文件原型整条链点得通、跨视图联动、派生用 `derive()` 算、通过确定性测试。

> 收口：**概念识别让系统"装得下"业务；时刻设计让系统"好用到不可替代"。** 前者定对错，后者定生死。

---

## 附录 A · 空白模板合集（一次性复制）

```
【1 实体链】
__事实__ ──> __引力中心__ ──> __连接__ ──> __周期容器__   ＋ 旁挂 Event

【2 字段二分】实体 | 事实字段(只读) | 运营字段(可写)

【3 状态流转】当前状态 | 下一状态 | 触发动作 | 谁能触发

【4 角色×时刻网格】角色 | 性质 | 时刻 | 主动作 | 通知触发 | 隐含写权限

【5 视图收口】# | Tier-2 视图 | 服务角色·时刻 | 形态(卡片/表格)

【6 通知通道】触发事件 | 通知谁 | 落到哪个视图 | 通道(站内/digest/push)

【7 权限矩阵】角色 | 能进的视图 | 能按的写动作

【8 演示动线】一句话串起跨视图联动的 ~1 分钟路径
```

---

## 附录 B · 换一个域走一遍（ATS，证明方法不依赖半导体）

> 同一套流水线套到**招聘系统**。只走关键步，证明每条纪律都会被触发。

### B.1 阶段 1 概念识别

**实体链**：
```
候选人/简历(上游事实) ──(针对岗位投递)──> 投递 Application(引力中心·工作流) ──(需面试)──> 面试 Interview(排期/面试官/结论) ──(汇总)──> Offer 审批
                                                  │
                                          事件 Event（阶段跃迁审计："这人为什么挂了"）
```

- **引力中心 = 投递 Application**（候选人针对某岗位的一次投递）——招聘官每天驱动的就是它，不是"候选人"（候选人多是上游只读事实）。
- **事实字段**：姓名/学历/渠道来源/简历正文（上游/不可改）。**运营字段**：当前阶段、评价、面试反馈、淘汰原因。
- **状态枚举**（≤6）：待筛选 → 面试中 → Offer审批中 → 已发Offer → 入职 / 已淘汰。
- **多级 = 游标**：Offer 审批走 HRBP → 用人经理 → HR VP，是**同一个"Offer审批中"+ `current_level`**，不是 3 个状态。
- **派生视图**（非概念）：待我筛选、待我反馈、待我审批、本周面试 —— 都是 `f(投递+规则+事件)`。

### B.2 阶段 2 时刻设计（角色 × 时刻网格）

| 角色 | 性质 | 时刻 | 主动作（一键） | 通知触发 | 写权限 |
|---|---|---|---|---|---|
| **招聘官** | 高频·主角色 | 入口·筛新投递 | 进面 / 淘汰 / 加备选 | "有新投递待筛" | 投递运营字段 |
| **招聘官** | 高频 | 推进·处理我的投递 | 约面 / 推进Offer / 标淘汰 | "投递卡住/超时未跟进" | 自己名下投递 |
| **招聘官** | 高频 | 卡点·查来龙去脉 | （只读：渠道→筛→面→反馈链） | — | 只读 Event |
| **面试官** | 事件驱动·低频 | 轮到我·交面试反馈 | 提交评价（通过/拒） | "你有面试待反馈" | 该轮面试反馈 |
| **用人经理** | 事件驱动·低频 | 轮到我·审 Offer | 批准 / 打回（带意见） | "有 Offer 待你审批" | current_level 命中时 |
| **协调员** | 周期驱动 | 会前·排本周面试 | 确认排期 / 改期 | "面试 T-1 提醒" | 面试排期 |
| **协调员** | 周期驱动 | 会中·跟当日面试 | 记结论 / 需补面 | "面试开始" | 面试结论；补面=新建 |
| **管理员** | 第二用户类型 | 看渠道接入健康 + 维护职位/审批阶梯 | 重试同步 / 改参考数据 | "渠道同步失败" | 后端配置 + 参考数据 |
| **跨角色** | 早间 | 看冲我来的事 | 点击直达对应视图 | digest + push | 只看自己被卷入的 |

**收口 ~7 个 Tier-2 视图**：筛选收件箱 / 投递工作台 / 面试反馈待办 / 待我审Offer(whose-turn) / 本周面试看板 / 当日面试议程 / 通知中心 —— 与半导体范例**结构同构**。

### B.3 哪条纪律被触发了（自检映射）

| 纪律 | 在 ATS 里的体现 |
|---|---|
| 引力中心非数据量最大 | 选"投递"而非"候选人" |
| 事实 vs 运营 | 简历只读，反馈/阶段可写 |
| 多级=游标 | Offer 三级审批 = 1 状态 + 游标 |
| 重排=新建 | 补面 = 新建面试记录，旧的留痕 |
| 派生≠概念 | 待我筛选/反馈/审批都是过滤视图 |
| 低频派生 | 面试官/用人经理 = 通知 + whose-turn，无独立 dashboard |
| 形态即契约 | 收件箱/工作台用卡片；候选人全库用 Tier-1 表 |

> 结论：换了域，**实体名全变、结构和纪律一字不改**。这就是方法可迁移的证明——你下一个产品照这条流水线走即可。
