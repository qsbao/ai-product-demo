# 运营管理系统：领域模型与设计决策

> 半导体工艺运营管理系统。本质：**事务运营引擎**——CRUD 表格是皮，"自动算 / 推 / 攒"的规则才是命。

## 核心实体链

```
事实告警 Alarm ──(聚合/升级)──> Issue ──(需上会)──> 议题 Topic ──(排期于)──> 会议 Meeting
  (上游只读事实+              (引力中心,         (Issue×Meeting              (日期/议程/
   轻量运营层)                工作流/生命周期)     的带生命周期连接,            状态)
                                ↓                  原"Report"概念改名)
                              报告 Report
                              (1:1 Issue, 外部权威源:
                               url + 状态薄壳)

                    事件 Event（一等公民，旁挂的 append-only 时间线/审计）
```

## 锁定的设计决策

### 1. 引力中心是 Issue
人驱动的是 **Issue**，不是告警。Alarm 是只读证据（evidence），只配**只读展示详情**，不配操作工作台；Issue 才配富工作台。
> 心法：详情页的可操作程度，取决于"这个概念上发生多少人的操作"，而非"它有多少属性"。

### 2. 数据来源：上游事实 + 运营层叠加
- 事实数据从上游自动流入（只读，同步会覆盖）；人只在其上叠加**运营层**。
- 模型里显式区分 **事实字段（只读、有来源）** vs **运营字段（人可写、本系统拥有）**。
- 接入层放后台 / 给管理员（**第二类用户**）。

**Issue 来源不止分诊**：`Issue.source` 是事实字段，取值 `triage` / `external-<系统名>`（如 `external-MES`、`external-SPC`），硬编码维护（呼应决策 #3）。分诊升级的 Issue 同时保留 `sourceAlarmId` 指向证据；外部派单的 Issue `sourceAlarmId` 为空，但仍可关联告警。**工作台是所有 Issue 的家，不是分诊的下游**——外部派单跳过分诊直接落到 owner 名下，靠通知卷入。

### 3. 规则一律硬编码
SLA / 超时 / 升级策略 / 汇报节奏——集中、显式、由团队改代码 + 发版。
> **不做规则引擎、不做配置 UI**，直到拿到"高频变更 × 必须业务方自助"的铁证。配置化的成本应由"变更频率 × 自助需求"偿付。

### 4. 工作流：状态枚举，不是引擎
已有的工作流引擎要**干掉**（开发负担 + 向用户暴露流程拓扑导致 confuse）。替代方案：

- **少量状态枚举**（处理中 → 审批中 → 已解决 → 已关闭，驳回退回处理中）
- **一张合法流转表**（硬编码）
- **路由数据**：当前审批层级游标 + 审批阶梯（引用数据）

> 多级审批（四级 / 五级经理）**不是更多状态**，而是同一个"审批中"状态 + 一个 `current_level` 游标。**阶段(phase)才配当状态，迭代(iteration)只配当数据。** 别把数据建模成拓扑。

**「已解决」是有持续时长的相位，不是 0 时长的中转**：只在 `必须上会=✓` 路径出现（"等上会"相位），✗ 路径直接从 审批中 跳 已关闭——0 时长不立持久态，否则就违反"阶段才配当状态"。详见决策 #9 的路径分叉。

**自动推进**：用户不为状态推进多点一下。审批通过 → 自动到下一态（已解决 或 已关闭）；上会·成功 → 自动到 已关闭。状态机被事件驱动，不是按钮驱动。

### 5. 审批人：解析自组织图，创建时快照
审批人 = `resolve(指派工程师 → 沿汇报线上溯 → 四级/五级)`。
- 工程师经"站点 → 站点负责人"登记表指派（本系统自有引用数据）；
- 组织层级来自上游 HR（只读，别在本系统维护人事）；
- **创建 Issue 时**对整条链一次性快照，连同"为何是此人"落进创建事件；改派暂不考虑。
- **审批留痕**：打回必填理由（已有），批准可选 comment；两者都落进 approve/reject 决策事件 body（决策 #6），不新增字段、不新增视图。

### 6. 事件：A 型（时间线/审计），不是事件溯源
- **做 A**：实体照常存状态字段，**额外**追加不可变事件流。喂养详情页时间线、派生清单、问责审计。
- **不做 B**（Event Sourcing）：对运营系统是灾难性过度设计。
- 只有 **生命周期跃迁 + 人的决策** 配当一等事件；普通字段编辑走通用 audit 日志。

### 7. 议题 / Meeting

> **改名说明**：原 "汇报 Report" 概念整体改名为 **议题 Topic**——它本来就是"Issue × Meeting 的调度实例"，叫 Report 与真实存在的 8D/FA 报告文档（决策 #11 引入）撞名。生命周期、字段、自动排期、Meeting 删除禁令**完全沿用**，只换名字。

- **会议 Meeting 是一等概念**（日期 / 类型 / 议程 / 状态）。
- **议题 Topic 是 Issue 与 Meeting 之间带生命周期的连接**：`Issue 1:N Topic`、`Meeting 1:N Topic`，状态 待汇报 / 成功 / 需重排 / 驳回 / 移出。
- **重排生成新议题记录，不改旧的**（保留历史："这个 Issue 汇报三次才拍板"自然留痕）。
- **驳回 ≠ 需重排**（严格区分）：`需重排`=时间问题（议程没排上 / 临时改期）；`驳回`=结论问题（评审会判定结论不成立）。驳回触发 Issue 从 `已解决` 退回 `处理中`，记一条「议题驳回」决策事件（理由必填，对称于审批驳回 `审批中→处理中`）。复盘后重新走流程 → 新议题，旧的留作驳回历史。
- **任何改议题的挂会动作都算重排**（跨日 / 同日早→晚都一样）：新建议题、旧议题标 `需重排`。一致性 > 便利性——"本来早上要讲、临时挪晚上"也是有意义的历史。

**Meeting 字段**：
- `date`（事实，自然日，**不存时分**——团队共识）
- `type`（事实，硬编码枚举：`早会 / 晚会 / 专题 / 其他`，呼应决策 #3）
- `status`（运营：`待召开 / 已开 / 取消`）

**Meeting 来源**：
- **系统自动滚动建**：周一至周六每天 2 条（早会 + 晚会），周日不建，节假日暂不处理
- **会议主持人手动建**：专题 / 其他 类型可手动建
- **会议主持人可删未开过的 Meeting**——但**禁止删任何已挂议题的 Meeting**（含待汇报/成功/驳回/需重排），强制先把议题重排走或将 Meeting 置为「取消」状态。这与决策 #6 时间线/审计一致。

**自动排期算法**（在 Issue 自动建议题时同事务执行，承接决策 #9 的 ✓ 路径）：
```
1. 输入：Issue.occurredAt
2. window_day = (occurredAt − 13:00).date()   // 13:00 分水岭对齐自然日
3. 目标 = window_day + 2 天 的 type=早会 Meeting
4. 兜底：目标不存在/已开过 → 顺延到下一个仍未开的早会（处理周日 / 慢解决 / 边界）
5. 仍无可用早会 → Topic.meetingId=null，落「待排期」派生视图（决策 #8）
```
举例：T=周五 10:00 → 周六早会；T=周五 14:00 → 周日早会不存在 → 周一早会；T=周六 14:00 → 周一早会；T=三天前的 Issue 拖到今天才解决 → 顺延到下一个未开早会。

**自动排期标记**：`Topic.scheduledBy ∈ {auto, manual}`（事实字段，决策 #2 区分）。后续若 manual 比例飙升，说明规则需要复议（决策 #3 "频率 × 自助需求"偿付的信号）。

**手动排期/重排（会议主持人）**：所有人工挪动都按"新建议题、旧标 `需重排`"，必填理由落决策事件（决策 #6）。

**派生视图**：
- 「待排期」= `Topic where meetingId is null and status='待汇报'`
- 「待汇报清单」= `Topic where status='待汇报' and meetingId is not null`

> 视图名"待汇报清单"保留——"汇报"在这里是 owner 视角的工作动词（"这条议题等着汇报"），换"待上会清单"反而把视角偏到调度侧。议题与报告文档（决策 #11）生命周期**解耦**，议题创建/排期不卡报告就绪状态。

### 8. 派生视图 vs 概念
凡 `f(实体 + 规则 + 事件)` 算出、无独立生命周期的（超时清单 / 升级清单 / 待汇报 / 上会清单）= **过滤视图，不是概念**。别把视图硬做成表。

### 9. Kanban / Risk / 必须上会：因子分层 + 派生门禁 + 可覆盖
「Issue 值不值得让评审会过一遍」是**多因子规则派生**，不是单字段拍板。模型分三层：

**层 A · 可覆盖因子（运营字段：auto + 人工 override + 必填理由）**
- **Kanban**（布尔，重要度信号）：受影响批次中任一 `lot.priority == 0`（hot lot）→ 默认 Kanban；
- **Risk**（枚举：低 / 中 / 高）：默认按上游量测/严重度的简单映射（先粗后细，对齐决策 #3 不做配置 UI）。

**层 B · 上游事实（决策 #2 的事实字段，只读）**
- `lot.subType`（在 lot 上，取值 A / AA / AE / B / ……）；
- `chartLevel`（在 issue 上，取值 KIP / ACP / ……；一条 issue 对应一个主控图层级）。

**层 C · 派生标志（不可独立覆盖）**
- **必须上会** = `Kanban ∧ (∃ lot: subType ∈ {A,AA,AE}) ∧ (chartLevel = KIP ∨ (chartLevel = ACP ∧ risk ∈ {中,高}))`
- 子批用 **any-lot 满足**（任意一个受影响批次命中即满足，避免被低重要度 lot 稀释）。

**终结路径分叉**（同一组状态枚举，**只是允许的转移不同**）：
- **必须上会 = ✓**：`审批中 →(自动) 已解决 →(自动建议题，自动排期)→ 上会·成功 →(自动) 已关闭`；`已解决` 是真实"等上会"相位，有持续时长。
- **必须上会 = ✗**：`审批中 →(自动) 已关闭`，跳过 已解决（0 时长不立持久态）。

**没有"发起汇报"按钮**：必须上会=✓ 的 Issue 在审批通过的同事务里，系统**自动建 `待汇报` 议题**——没有"是否要发起"的语义余地，所以不暴露这一步。用户的下一个手动动作是「排期」（如果自动排期未命中），不是发起。议题自动按决策 #7 的排期算法挂到目标早会，无可用早会则落「待排期」派生视图。

**Override 落在因子层（Kanban / Risk），不在派生层（必须上会）**：
- 想改派生结果，强制落到具体因子上——「我觉得 Kanban 误判了」或「风险其实是高」，理由就是对**那一个事实/判断**的复议，可追溯；
- 派生结果若被独立覆盖，理由变成对结果的复议，丢了语义、堆出灰色地带；
- Trade-off：用户没有「绕过规则但说不清原因」的快捷出口——但这正是决策 #3 的纪律。

**允许时机**：已关闭前任何阶段；工程师（owner）+ 审批经理可改 Kanban / Risk。
**留痕**：改因子不产生新状态——只改字段 + 落决策 event（决策 #6 的"人的决策"配当一等事件）。
**级联**：覆盖导致 **必须上会** 由 ✓ → ✗ 时：
- 若 Issue 仍在 处理中 / 审批中：仅切换后续路径（终结时直关），不动当前态；
- 若 Issue 已在 **已解决**（"等上会"相位变得无家可归）：**自动**从 已解决 跳 已关闭——不弹"确认关闭"。理由：翻转因子本就必填理由、问责已落人，与"自动推进"哲学一致；不自动关的话，已解决 在 ✗ 路径下没有合法去处；
- 已存在「待汇报」议题 → 自动标 `移出`（保留历史）；**报告文档不动**（决策 #11，文档与议题生命周期解耦）。

> 心法呼应决策 #4：**阶段 (phase) 是状态、重要度 / 风险是数据**。别把「重要」或「风险」做成状态机。
> 工作台也别对所有终结后的 Issue 长留——默认隐藏「已关闭」，给个「含已关闭」开关给工程师回看自己结过的案。

### 10. 诊断字段：工程师对 Issue 的结构化结论（不是 comment）

**两个运营字段，前缀 `diagnosis_*`** —— 与上游事实字段、外部系统的 detection 字段在词法上区隔，表明这是「工程师对本 Issue 的诊断」：

- `diagnosisSubtype`（枚举、受控、硬编码维护，呼应决策 #3 不做配置 UI）：示例值表 `工艺漂移 / 设备异常 / 物料批次 / 量测误差 / 软件配置 / 其他`（团队定，发版改）。
- `diagnosisDetection`（短文本，≤200 字）：「怎么发现的 + 关键观测」的工程师自述。

**必填时机**：「提交审批」主动作——审批经理审的就是这份结论，没诊断不让进审批流；未填则提交按钮弹收集弹窗，填好后一并提交。
**可提前填**：处理中阶段任何时候，owner 可在详情抽屉/详情页里直接填，工作时随手记。
**可改时机**：已关闭前任何阶段，**owner + 审批经理可改**（与决策 #9 因子覆盖的权限模式一致）；修订必填理由，每次变更算「人的决策」配当一等事件（呼应决策 #6），喂事件时间线 + 后续工艺归因分析。**首次填写不必填理由**（自然的工作产出，不是修订）。

> **不做自由 comment / 万能评论框**。跨角色沟通走通知 + 事件流（决策 #6 的双轨：人的决策→一等事件，普通字段编辑→通用 audit 日志），避免退化成 Jira 评论流——审计价值才不会被闲聊稀释。
>
> 这俩字段**和审批 comment 是不同性质**：诊断是结构化的工艺归因数据，可分析、可统计；审批 comment 只是决策事件 body 上的说明文本。别混到一个万能字段里。

### 11. 报告 Report：1:1 Issue 的外部权威源薄壳

**分家说明**：原 "Report" 概念是 "Issue × Meeting 的调度实例"，本次改名 **议题 Topic**（决策 #7）。现在 "Report" 这个词归还给它的字面意思——**8D / FA 报告文档本身**，活在外部专门的报告服务里，本系统只持一个薄壳。

**为什么拆**：一份 Issue 写一份文档（外部编辑、多版本），但可上多次会（重排 → 新议题）。两个生命周期捏在一起会让"重排=新建 Report"与"文档这次到底改了没"互相污染语义。

**字段**（运营层薄壳；事实归外部服务）：
- `id`（本系统内键，前缀 `RPT-xxx`）
- `issueId`（**唯一**，1:1）
- `url`（外部报告服务里的编辑链接，**事实字段**·外部权威）
- `status ∈ {已生成, 生成失败}`（注：**不要**"待生成"占位——异步场景里多噪音，要么有 url 要么没）
- `createdAt`、`lastTriedAt`
- `createdBy ∈ {auto-external, manual-retry}`

**生命周期**：
- **路径 A · 外部自动**：Issue 创建事件触发外部报告服务（系统 POST / 服务订阅），服务异步回调本系统写入 Report 记录（status=已生成 + url）。
- **路径 B · 手动重试**：UI 显示「生成报告」按钮，条件 = `Issue 没 Report 记录 ∨ Report.status=生成失败`。点击 → 本系统调外部服务一次 → 成功写入/更新 url + status=已生成；失败标 status=生成失败 + 更新 lastTriedAt。

**与议题的耦合时机** = "上会那一刻"：议题该建该排都按决策 #7 节奏走，不卡报告就绪。进入"会议议程"视图时，每条议题旁带"报告 ready/未 ready" mini-badge——主持人会前可催 owner，但**不阻挡上会**。呼应决策 #3 "不治未加之病的门禁"。

**与议题版本的关系**：议题**不快照**报告版本号——版本归外部系统管，"三周后回看那场会汇报的是哪个版本"靠外部时间戳对齐，本系统不复刻。

**权限**（沿用决策 #10 一致性）：所有角色可查、可点击跳外部；只有 owner + 审批经理可点重试。

**事件**（决策 #6 一等事件）：
- 「自动建报告」（系统）—— 外部服务回调成功
- 「报告生成失败」（系统）—— 外部服务报错或超时
- 「手动重试报告」（owner / 审批经理）—— 含 url 变化时记录新链

**派生视图**：暂不独立建"待生成报告"视图（决策 #8 派生不是概念）。Issue 详情 banner + 工作台行内 mini-badge 已能覆盖。若 owner 后续要"批量查看我的报告缺口"再补。

> **报告 vs 诊断**：报告是外部自由文档（PPT/Wiki），诊断是本系统结构化归因（决策 #10）。**互不替代**——诊断可能预填进报告文档的某一段，但本系统只对结构化字段做事实留痕，文档变更走外部版本系统。
