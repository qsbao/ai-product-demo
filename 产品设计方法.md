# 产品设计方法：架构师如何在没有 PM 的情况下做产品

> 适用背景：资深架构师带小团队，为半导体工艺运营做内部管理系统，无专职产品经理。

## 一个公式

> **产品设计 = 概念识别（名词）＋ 时刻设计（动词）**

| | 概念识别（已会） | 时刻设计（要补） |
|---|---|---|
| 回答的问题 | 系统里**有什么** | 用户此刻**要做什么** |
| 产出 | 领域模型：实体 / 关系 / 生命周期 / 规则 | 角色 × 时刻 → 视图清单 |
| 决定的是 | **对错**（装不装得下业务） | **生死**（好不好用，打不打得过 Excel） |
| 对应 UI | Tier 1 通用 CRUD（长尾、低价值） | Tier 2 定制视图（少数、高价值） |

核心认知：**你缺的不是第二个 PM，而是把"为每个角色设计他那一刻"当成一道和建模并列的独立工序。**

## 时刻设计的做法（四步，有限可收敛）

1. **列角色**：谁会打开这个系统。
2. **每个角色写 2–3 个高频时刻**，用叙事——"早上来了先干什么 → 然后 → 卡在哪"。
3. **每个时刻只问一句**："他此刻要做的那**一个**决定/动作是什么？" → 配一个**主动作一键可达** + **他需要看的最小信息** + **其余全部隐藏**。
4. **每个时刻造一个专注视图**（Tier 2），剩下的实体维护全丢给通用 CRUD（Tier 1）。

**做对了的判据**：

- 每个高频时刻 ≤ 1 个视图，主动作一键直达；
- 用户**不需要自己拼信息、自己记得去查**（系统主动算 / 推 / 攒）；
- 屏幕上没有他此刻用不到的东西。

## 两层 UI 预算

- **Tier 1 — 通用 CRUD 生成器**（长尾，约 60% 屏幕，约 10% 价值）：引用数据维护（站点 / 登记表）、管理员视图、原始记录只读查看、简单实体。这一层"概念 → 表 → 详情"的赌注**完全正确**，生成器造一次到处用。
- **Tier 2 — 定制"时刻"视图**（约 40% 屏幕，约 90% 价值）：本系统至少有 7 个——告警 triage 收件箱、派生仪表盘、审批"轮到谁"视图、Issue 工作台、事件时间线、会议议程、通知推送。逐个手工打造。

> 常见误区：以为纯 CRUD 能覆盖一切。错的恰恰是这 40%——它是你战胜 Excel / Jira 的全部理由。

### 形态即层级契约（哪层长什么样，不由做的人当场拍脑袋）

两层不只是"做什么"不同，**长什么样**也要固定成两套视觉语言——形态本身是契约：用户还没读字，就该从样子上知道"我在哪层、此刻该干嘛"。

| | Tier 2 时刻视图 | Tier 1 实体表 |
|---|---|---|
| 形态 | 卡片 / 行列表，每行**一个主按钮**，详情折叠 | 密集表格，多列，可排序 / 筛选 / 导出 |
| 优化的认知任务 | **决策 + 动作**（视线落在按钮） | **定位 + 比较**（视线扫列） |
| 可写性 | 在此动手 | 行只读，点行进详情/工作台才动手 |

为什么值得当纪律：

- **省重学**：全 app 只需学一次"卡片=动手、表格=查数"，每进新屏不必重新判断用途。
- **任务不可混用**：表格做时刻视图→一键主动作淹没在列里；卡片堆千条记录→没法扫/比/筛。
- **防滑回 Excel**：让时刻视图**刻意不像表格**，是用视觉重力对抗"什么都做成一张表"的滑坡——形态上做不出来就不易越界。
- **标记只读 vs 可写**：呼应权限副产物（事实字段只读、运营字段可写）。
- **同一实体多入口靠形态+分组区分**：如 Issue 既有 Tier-2 工作台又有 Tier-1 全表，两个列表层按层级分形态、**详情层共享同一抽屉**——这就是两层的接缝。

平衡：不是"表格不好"，表格对 Tier-1 是对的；讲究的是**让形态匹配任务、并把形态差异当信号用**，别矫枉过正到什么都做成卡片。规则一句话：**一个新屏长什么样，由它属于哪一层决定。**

#### 机制：形态为什么能起作用——它在控制用户的视线

形态不是审美，是把认知成本写进布局：**视线走的路径决定完成任务要几次"眼跳"**。能控制视线的杠杆就那几个——

| 杠杆 | 时刻视图（要"动手"） | 实体表（要"找/比"） |
|---|---|---|
| 颜色饱和 | 只给主动作上饱和色 → 当"视线磁铁" | 几乎不用，保持平静好扫 |
| 位置 | 该动手的放左上、动作放右端（行的终点） | 表头在顶当标签锚 |
| 对齐 | 标题左对齐 → 稳定左边缘往下扫 | 数字右对齐 → 对位比大小 |
| 排序 / 筛选 | **最强**：把答案排到眼睛落点，免扫 | 列排序 / 搜索做定点定位 |
| 密度 | 低密度，每条给一次注视 | 高密度，进入扫描模式 |

两个对照例（本系统）：

- **分诊收件箱（卡片）**：左侧严重度色点(前注意先跳进眼) → 左对齐标题 → 整行**唯一饱和蓝**的"升级"按钮(视线终点)。每行 1–2 次注视完成"看清→动手"。更高级的是 **Issue 工作台**：把"该我动的"**置顶排序**，视线天然落在左上角就是答案——排序替用户把扫描做完了，这是优化的天花板。
- **Issue 全表（表格）**：找一条沿某一列纵向扫、读一行横向跨列回看表头。**关键反推**：行内**故意不放饱和色动作按钮**——否则前注意的"跳"触发几十次，把本该平静的比较界面搅烂。所以行只读、动作延后到抽屉。是"比较"这个视线任务决定了这里不能有磁铁，而非美观。

一句话：**形态决定视线路径，视线路径决定任务效率。** 选卡片还是表格，本质是在选"优化哪种眼动"，而它必须匹配任务是"动手"还是"找东西"。

#### 延伸：抽屉 vs 详情页——由任务决定，不由内容量决定

一个高频的实操问题：实体（如 Issue）要展示的字段特别多，抽屉装得下吗？要不要做整页详情页？

**容易犯的错是用"内容量"做判据**——"字段多就上整页"。正确的判据仍是**用户此刻在干嘛**：

| | 抽屉（Tier-2 时刻配件） | 详情页（Tier-1 规范记录） |
|---|---|---|
| 服务的时刻 | **推进**——在我的清单里顺手处理这一条 | **研究 / 追溯 / 分享 / 打印**——停下来吃透或对外引用一条 |
| 核心价值 | **不离开上下文**：ESC 即回队列，接着下一条 | **独立地址**：可收藏 / 分享 / 打印 / 并排比对 |
| 内容策略 | 按此刻裁剪 + **分页签**，低频字段折叠 | 全字段全展开，不裁剪 |
| 入口 | 从工作台 / 收件箱 / 通知点开 | 从全表、深链、或抽屉里的"展开整页" |

要点：

- **内容多，先用渐进披露解决，而不是换载体**。抽屉纵向空间很大，用 概览 / 事件 / 关联 / 讨论 分页签 + 段内分区，几十个字段也扛得住——抽屉的价值是"不丢上下文"，不该因为内容多就轻易放弃。
- **真正逼出整页的不是字段数，是任务变了**：要深链/分享、要打印归档、要并排比对、要超重内容（大量附件/关联）——这些抽屉结构性扛不住，才升级到整页。这就是 Tier-1 的"表→详情"，和 CRUD 生成器的赌注一致。
- **一份内容，两种载体，别写两套**：用同一组"区块渲染器"，抽屉调它（裁剪+分页签）、详情页也调它（全量展开），区别只在布局壳。否则字段一改两处漂移。
- **形态依旧在说话**：浮层=在流程里顺手动手，整屏=停下来研究一条记录——延续"形态即层级契约"。

> 原型已落这条：Issue 抽屉做成 4 个分页签（概览/事件/关联/讨论），底部常驻"⤢ 展开为整页"，点开进入与抽屉**共用区块渲染器**的 Issue 详情页（带模拟深链）。

## 落到本系统：角色 × 时刻提纲（待填）

| 角色 | 高频时刻（待细化） | 对应 Tier 2 视图 |
|---|---|---|
| 工程师 | 我名下哪个 Issue 该我动、卡在哪一步 | **"我的工作台"** |
| 审批经理（四 / 五级） | 轮到我批的有哪些 | **审批收件箱**（批 / 驳一键，藏拓扑） |
| 会议主持人 / 协调者 | 这周要上会的、排了没、还剩哪些 | **会议议程 / 待汇报清单** |
| 管理员（本团队） | 数据源、规则、登记表 | 通用 CRUD + 配置 |
| 跨角色（早间） | 新告警 / 超时 / 已升级 | **triage 收件箱 + 派生仪表盘** |

## 一句话收口

**概念识别让系统"装得下"业务；时刻设计让系统"好用到不可替代"。** 时刻设计会顺带牵出"通知通道"和"权限"两个附属物，但那是它的副产品，不是新学科。
