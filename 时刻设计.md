# 时刻设计：角色 × 时刻网格

> 这是「产品设计 = 概念识别 + 时刻设计」里**动词**那一半的产出。
> 概念识别已完成（见 [领域模型.md](领域模型.md)）。本文档把每个角色的高频时刻走一遍，
> 收口成 Tier 2 的 ~7 个定制视图，并让**通知**与**权限**两个副产物自动浮现。
> 见 [产品设计方法.md](产品设计方法.md)。

---

## 一、方法：怎么做时刻设计（架构师无 PM 版）

时刻设计不是"给每个角色画几个页面"。它是一条有**严格依赖顺序**的推导，每一步都砍掉一堆看似合理实则有害的设计。

**纪律六步：**

1. **选主角色 —— 对齐重力中心。** 不要 5 个角色平行铺开（那会掉进"每个角色一个 dashboard"的对称陷阱）。选**和领域模型重力中心对齐的那个高频角色**，先把它的时刻链打穿，其余角色派生。
2. **沿概念链走主角色的"入口 → 推进 → 卡点"。** 早上来先干什么（入口）→ 然后干什么（推进）→ 卡在哪（卡点）。链条要贴着 `Alarm → Issue → 议题 → Meeting`（+ 旁挂 报告，1:1 Issue）走，因为人的动作就是在推这条链。
3. **低频角色按"如何被卷入主角色的时刻"派生。** 审批经理 / 会议主持人不是每天主动来的，他们是被 Issue 状态变更**卷入**的。他们的最佳形态是"一条通知 + 一个 whose-turn（轮到我）视图"，不是独立 dashboard。
4. **每个时刻 → 一个聚焦视图。** 主动作一键完成，其余全部隐藏/折叠。落地页只能有一个赢家，不许塞 widget。
5. **收口去重。** 把所有时刻视图去重，会自然收敛到 ~7 个 Tier 2 视图（多个角色共享同一视图）。剩下的长尾掉进 Tier 1 CRUD。
6. **让副产物浮现。** 通知通道 = 第 3 步里所有"把人卷回来"的触发点的集合；权限 = 每个视图只暴露该时刻需要的写动作。两者都不需要单独立项。

**这套方法主动规避的反模式：**

| 反模式 | 表现 | 为什么有害 |
|--------|------|-----------|
| 对称陷阱 | 给每个角色发一个 dashboard | 低频角色根本不会来看，价值为零，还稀释主角色的设计 |
| widget 堆砌 | 落地页塞满"我的告警/我的待办/我的汇报"卡片 | 退化成又一个 Excel/Jira，没有"一键主动作" |
| 视图升表 | 把"超时清单/待汇报"做成实体表 | 它们是 f(实体+规则+事件)，是派生视图，不是概念 |
| 状态爆炸 | 把四级/五级审批做成 5 个状态 | 应是 1 个"审批中"状态 + level 游标（阶段是状态，迭代是数据） |
| 低频当高频 | 给审批经理设计"早间入口" | 他没有早间例程，他是被通知拉进来的 |

---

## 二、案例推演：一步步走完每个角色

### 主角色锁定：工程师

为什么是工程师（而非告警值班、班组长）：

- 域模型里 **Issue 是重力中心**，而**驱动 Issue 的人就是工程师**——Alarm 是只读证据，审批/上会/管理都是 Issue 状态推进的下游。主角色必须和重力中心对齐。
- 工程师是**高频**（每天反复进出）；审批经理/会议主持人是**事件/周期驱动**的低频角色。
- 工程师的时刻链跑不通，系统就输给了 Excel/Jira——这是决定生死的那条线。

#### 工程师的时刻链：入口 → 推进 → 卡点

| 阶段 | 时刻 | 早上来先干什么 / 卡在哪 |
|------|------|------------------------|
| **入口** | 分诊新告警 | "昨晚到今早冲我 team 来了哪些告警？哪些要升成 Issue，哪些 ack，哪些误报忽略？" |
| **推进** | 处理我 team 的 Issue | "我 team 现在哪些 Issue 需要动手？把它推到下一个状态（提交审批/补充/标记解决）——同事 owner 的也算我的，所有权共享。" |
| **卡点** | 查来龙去脉 / 被打回 | "这条 Issue 怎么走到今天的？审批为什么被打回？汇报为什么要重排？" |

> **Issue 入口不止分诊**——分诊收件箱、自动规则升级、**外部系统派单**（MES/SPC 等）三条入口都通向 Issue 工作台。**工作台是所有 Issue 的家**：外部派单的 Issue 跳过分诊直接落到 owner 名下，靠通知卷入；分诊收件箱仅是入口之一。

- **入口 → 分诊收件箱**：半自动。规则能自动聚合的（同站点同类告警）自动升级；**只有机器拿不准的才进分诊池**。每条一行，主按钮三选一：升级为 Issue / Ack / 忽略。消费 Alarm 那层薄运营字段（ack 布尔 + disposition 枚举）。**默认 team 视图**——按 `alarm.owner`（site→owner 解析）→`owner.team`（engineer→team 注册表，**动态 lookup**，因为 alarm 尚未升 Issue、team 还没 snapshot 的时机）过滤；顶部范围切换器可切「仅我的」（owner=me）。owner 作为可见列。
- **推进 → Issue 工作台**：域模型里 Issue 独有的**富工作台**。**默认 team 视图**——展示登录用户所属 team（创建时 snapshot）名下全部 Issue，体现所有权共享；顶部范围切换器可一键过滤到「仅我的」（assignee=me）。assignee 作为可见列。默认按团队卡点优先级排序（被打回的、快超时的置顶）。主动作随状态变化为单个按钮：**提交审批（必填 `diagnosisSubtype` + `diagnosisDetection`——审批经理审的就是这份结论；未填弹窗收集，填好后一并提交）** / 补充信息 / 标记解决 / **关闭（仅非必须上会）**。处理中阶段可在详情抽屉里随手提前填诊断，工作时记下；后续 owner / 审批经理可改，修订必填理由、落决策事件。
- **派生门禁与因子覆盖（呼应领域决策 #9）**：工作台多一个「必须上会」切片（多因子派生 = Kanban ∧ 子批∈{A,AA,AE} ∧ (主控图=KIP ∨ ACP+风险≥中)），以及「含已关闭」开关回看终结后的 Issue。
  - **必须上会** 在「已解决」时**无主动作**——系统在审批通过同事务**自动建议题 + 自动排期**（决策 #7/#9），上会·成功自动关闭。已解决是"等上会"相位，工程师等结果。
  - **非必须上会** 路径**审批通过直接关闭**（跳过已解决，0 时长不立持久态）。
  - **覆盖入口在详情页/抽屉、落在因子层**：owner 和审批经理可改 Kanban、Risk 因子（必填理由，落决策事件）；主控图与子批是上游事实，只读。若覆盖导致「必须上会」由 ✓ → ✗ 且已有待汇报议题，自动移出（报告文档不动）。
- **卡点 → 事件时间线**（嵌在 Issue 详情里）：append-only，只读追溯。只读，不是动作面板——卡点的解法是"看清楚"，动作回到工作台完成。

→ 工程师产出 **2 个独立视图（分诊收件箱、Issue 工作台）+ 1 个嵌入组件（事件时间线）**。

### 派生角色：审批经理（事件驱动，低频）

没有早间例程。被"工程师提交审批"这个状态变更**卷入**。

- **唯一时刻：轮到我审批。** 收到通知 → 打开"待我审批" → 每条 **批准（可选 comment）/ 打回（必填意见）**，都进 approve/reject 决策事件 body（不新增字段、不新增视图）。看到的是创建时**快照的审批链** + Issue 摘要（只读）。
- 多级审批不产生多视图：四级/五级经理看到的是同一个"待我审批"视图，区别只是 `current_level` 游标走到了谁。
- 卡点：信息不足 → 打回。打回 = 生成一个 Event + 通知回工程师（落到工程师工作台的"需我动作"置顶）。

→ 产出 **1 个视图：待我审批（whose-turn / 轮到谁）**。

### 派生角色：会议主持人（周期驱动，绕着 Meeting/议题）

围绕 `议题 → Meeting`。两个时刻，一前一中。

- **会前：准备上会。** 看下次会议要汇报哪些 Issue（待汇报清单），主动作：重排到别会。这是一个**派生看板**（Meeting↔议题 上的过滤视图）。议题旁带 📄 报告 ready/未 ready 角标，可催 owner 但不卡上会（决策 #11 解耦）。
- **会中：过议程。** 逐条过议题，记结论：成功 / 需重排 / 驳回。**重排创建新议题，绝不改旧的**（留历史）；驳回 = 结论问题，Issue 退回处理中。报告文档不动（决策 #11，外部权威源）。

→ 产出 **2 个视图：待汇报清单（派生看板）+ 会议议程**。

### 派生角色：管理员（第二用户类型，面向接入/后端）

域模型说接入层面向后端/管理员。管理员不碰业务 Issue，他维护"系统能不能跑"。

- **入口：看接入健康。** 上游事实数据同步是否正常？哪个源断了？主动作：重试同步 / 看错误。这是一个**派生看板**。
- **维护：参考数据。** site→owner 注册表、审批阶梯——这些是 **Tier 1 CRUD** 的主要消费场景。

→ 管理员是**证明 Tier 1 没白做**的角色：1 个派生看板（接入健康）+ 一批 Tier 1 CRUD（参考数据）。

### 跨角色早间：通知中心（粘合层，非角色）

"跨角色早间"不是一个角色，是**所有人**打开系统时的统一入口。

- **时刻：看冲着我来的事。** 聚合所有"需我动作"（新告警待分诊 / Issue 被打回 / 待我审批 / 待汇报），点击直达对应时刻视图。
- 它是把低频角色拉回系统的**通知通道的前端**，也是高频角色的早间例程起点。

→ 产出 **1 个视图：通知中心**。

---

## 三、最终产出

### 3.1 角色 × 时刻网格

| 角色 | 时刻 | Tier 2 视图 | 主动作（一键） | 通知触发 | 隐含写权限 |
|------|------|------------|---------------|----------|-----------|
| **工程师** | 入口·分诊新告警 | 分诊收件箱（默认 team 视图） | 升级 Issue / Ack / 忽略 | "有新告警待分诊（本 team）" | 本 team 范围 Alarm 运营字段、建 Issue（所有权共享：alarm.owner.team == my team 即可操作） |
| **工程师** | 推进·处理我 team 的 Issue | Issue 工作台（默认 team 视图） | **提交审批（填诊断字段）** / 补充 / 标记解决 / **关闭（非必须上会）** | "Issue 被打回 / 即将超时 / Kanban·风险变更 / **新外部 Issue 派到我名下**" | 本 team Issue 运营字段（**所有权共享**，assignee 仅为审批链锚点）+ **诊断字段 `diagnosis_*`**（提交审批时必填；处理中可提前填，后续修订必填理由）+ **Kanban / Risk 因子**（必填理由） |
| **工程师** | 卡点·查来龙去脉 | 事件时间线（嵌入） | （只读追溯） | — | 只读本 team Issue 的 Event |
| **审批经理** | 轮到我审批 | 待我审批（whose-turn） | **批准（可选 comment）/ 打回（必填意见）** | "有 Issue 待你审批" | 对 current_level 的 Issue approve/reject + **诊断字段 `diagnosis_*`** + **Kanban / Risk 因子**（修订必填理由） |
| **会议主持人** | 会前·准备上会 | 待汇报清单（派生看板） | 重排到别会 / 看 📄 报告就绪 | "下次会议 T-1 提醒" | 议题 状态、排期 |
| **会议主持人** | 会中·过议程 | 会议议程 | 记结论：成功 / 需重排 / 驳回 | "会议开始" | 议题 结论、建重排议题 |
| **Owner / 审批经理** | 报告未生成 | Issue 详情 banner（行内） | 「生成报告」按钮（调外部报告服务） | "报告生成失败" | 报告 url + 状态（决策 #11） |
| **管理员** | 入口·看接入健康 | 接入健康（派生看板） | 重试同步 / 看错误 | "数据源同步失败" | 后端接入配置 |
| **管理员** | 维护·参考数据 | （Tier 1 CRUD） | 增 / 删 / 改 | — | 参考数据写权限 |
| **跨角色** | 早间·看冲我来的事 | 通知中心 | 点击直达对应时刻视图 | 早间 digest + 实时 push | 只看到自己被卷入的项 |

### 3.2 收口：~7 个 Tier 2 视图（去重后）

| # | Tier 2 视图 | 服务的角色/时刻 | 形态 |
|---|------------|----------------|------|
| 1 | **分诊收件箱** | 工程师·入口 | 每条一行，主动作三选一 |
| 2 | **Issue 工作台** | 工程师·推进 | 富工作台，按"需我动作"排序 |
| 3 | **事件时间线** | 工程师·卡点（嵌 Issue 详情） | append-only 只读追溯 |
| 4 | **待我审批** | 审批经理 | whose-turn，level 游标 |
| 5 | **会议议程** | 会议主持人·会中 | 逐条过 Issue 记结论 |
| 6 | **派生看板** | 会议主持人·会前（待汇报）+ 管理员（接入健康）+ 超时/升级清单 | f(实体+规则+事件) 过滤视图族 |
| 7 | **通知中心** | 跨角色早间 | 统一入口，路由到 1-6 |

正好对齐 [产品设计方法.md](产品设计方法.md) 里预估的 ~7 个。剩余长尾（实体的增删改查、参考数据维护）全部由 **Tier 1 CRUD 生成器**兜底，主要消费者是管理员。

### 3.3 副产物一：通知通道（从网格"通知触发"列自动浮现）

通知不是单独设计的，它就是网格里**每一处"把人卷回系统"的触发点的集合**。

| 触发事件 | 通知谁 | 落到哪个时刻视图 | 通道 |
|----------|--------|-----------------|------|
| 新告警进分诊池 | 工程师 | 分诊收件箱 | 站内 + 早间 digest |
| **新外部 Issue 派到我名下** | 工程师（owner） | Issue 工作台（置顶） | 站内 + 实时 push（外部派单等同被卷入，需实时拉回） |
| Issue 提交审批 | 审批经理 | 待我审批 | 站内 + 外部 push（低频角色，必须外部触达） |
| 审批被打回 | 工程师 | Issue 工作台（置顶） | 站内 + 实时 push |
| Issue 即将超时/升级 | 工程师 | Issue 工作台（置顶） | 站内 + 实时 push |
| 会议 T-1 | 会议主持人 | 待汇报清单 | 外部 push |
| 数据源同步失败 | 管理员 | 接入健康 | 外部 push（值守告警） |

原则：**高频角色用站内 + digest 即可；低频/卷入式角色（审批经理、会议主持人、管理员值守）必须外部触达**，否则他们不会回来。

### 3.4 副产物二：权限（从网格"隐含写权限"列自动浮现）

权限模型 = **视图可见性 + 该视图内动作的写权限**，不需要独立的 RBAC 设计。

- **事实字段全局只读**（来自上游，同步可能覆盖）；人只能写运营字段。
- 每个时刻视图**只暴露该时刻需要的那一个写动作**——这天然就是最小权限。
- 角色边界落在"能看到哪些时刻视图 + 能在视图里按哪个动作键"：
  - 工程师：分诊收件箱（**本 team 范围**，alarm.owner.team == my team；分诊场景下 team 由 owner 动态 lookup，因为尚未升 Issue）、Issue 工作台（**本 team 全部，所有权共享**——任何 team 成员对该 Issue 有完整操作权；assignee 仍是审批链锚点，且作为"仅我的"过滤器的依据）、通知中心。
  - 审批经理：待我审批（仅 current_level 命中自己时）、通知中心。
  - 会议主持人：待汇报清单、会议议程、通知中心。
  - 管理员：接入健康、Tier 1 CRUD（参考数据）、通知中心。

---

## 四、设计完成度自检

- [x] 主角色对齐重力中心（工程师 ↔ Issue）。
- [x] 每个角色 2-3 个高频时刻，无对称式 dashboard。
- [x] 每个时刻一个视图，主动作一键，其余隐藏。
- [x] 低频角色用 whose-turn + 通知派生，未独立设计入口。
- [x] 收口到 ~7 个 Tier 2 视图，长尾交给 Tier 1。
- [x] 通知与权限作为副产物从网格列直接读出，未单独立项。
- [x] 未把派生视图升为概念，未把多级审批做成多状态。

> 至此「概念识别 + 时刻设计」两半闭环：领域模型决定**正确性**，时刻网格决定**生存**。下一步进入实现——Tier 1 生成器 + 7 个 Tier 2 视图的搭建。
